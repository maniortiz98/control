import { HttpContextToken } from "@angular/common/http";

/* export const NO_AUTH = new HttpContextToken<boolean>(() => false); */
/* export const BASIC_AUTH = new HttpContextToken<boolean>(() => false); */
export const NO_LOADING = new HttpContextToken<boolean>(() => false);
