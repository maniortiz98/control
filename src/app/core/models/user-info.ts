export interface UserInfo {
    name: string;
    username: string;
    employeeId: string;
    localAccountId: string;
    homeAccountId: string;
    idToken: string;
    roles: Array<any>;
    rol: string;
}