import { Directorate } from "../../models/directorate";

// AuthorizedPersonCheckpoint[]
export function directorateMapToCheckpoint(data: Directorate[]): any {
    return data.map((per: Directorate) => {
        return {
        }
    });
}