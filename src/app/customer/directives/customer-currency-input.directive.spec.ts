import { CustomerCurrencyInputDirective } from './customer-currency-input.directive';

describe('CustomerCurrencyInputDirective', () => {
  it('should create an instance', () => {
    // const directive = new CustomerCurrencyInputDirective();
    // expect(directive).toBeTruthy();
  });
});
