import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CustomerFirstDataComponent } from './customer-first-data.component';

/* describe('CustomerFirstDataComponent', () => {
  let component: CustomerFirstDataComponent;
  let fixture: ComponentFixture<CustomerFirstDataComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [CustomerFirstDataComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(CustomerFirstDataComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
