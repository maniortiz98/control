import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CustomerNewCustomerComponent } from './customer-new-customer.component';

/* describe('CustomerNewCustomerComponent', () => {
  let component: CustomerNewCustomerComponent;
  let fixture: ComponentFixture<CustomerNewCustomerComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [CustomerNewCustomerComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(CustomerNewCustomerComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
}); */
