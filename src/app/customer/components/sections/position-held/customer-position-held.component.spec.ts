/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CustomerPositionHeldComponent } from './position-held.component';

describe('CustomerPositionHeldComponent', () => {
  let component: CustomerPositionHeldComponent;
  let fixture: ComponentFixture<CustomerPositionHeldComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [CustomerPositionHeldComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(CustomerPositionHeldComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 */
